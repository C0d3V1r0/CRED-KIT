/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  typedRoutes: false,
  devIndicators: false,
  compress: true,
  poweredByHeader: false,
};

export default nextConfig;
